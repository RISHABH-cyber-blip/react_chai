import { CDATA, ChildNode, Comment, DataNode, Document, Element, Node, NodeWithChildren, ParentNode, ProcessingInstruction, Text } from "./node.js";
//#region node_modules/domhandler/dist/index.d.ts
/**
 * Configuration options for `DomHandler`.
 */
interface DomHandlerOptions {
  /**
   * Add a `startIndex` property to nodes.
   * When the parser is used in a non-streaming fashion, `startIndex` is an integer
   * indicating the position of the start of the node in the document.
   * @default false
   */
  withStartIndices?: boolean;
  /**
   * Add an `endIndex` property to nodes.
   * When the parser is used in a non-streaming fashion, `endIndex` is an integer
   * indicating the position of the end of the node in the document.
   * @default false
   */
  withEndIndices?: boolean;
  /**
   * Treat the markup as XML.
   * @default false
   */
  xmlMode?: boolean;
}
//#endregion
export { CDATA, ChildNode, Comment, DataNode, Document, DomHandlerOptions, Element, Node, NodeWithChildren, ParentNode, ProcessingInstruction, Text };
//# sourceMappingURL=index.d.ts.map